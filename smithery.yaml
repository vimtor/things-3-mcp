runtime: "container"
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
startCommand:
  type: "http"
  configSchema:
    type: "object"
    properties:
      token:
        type: "string"
        description: "Things URL authentication token. Can be found in the Things 3 settings."
    required: ["token"]
  exampleConfig:
    token: "PMT3SfNhTnG_NpxIr4ML6U"
